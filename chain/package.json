{
  "name": "paydao-chain",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "anchor test --skip-build --skip-deploy --skip-local-validator"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^5.1.2",
    "ts-mocha": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
