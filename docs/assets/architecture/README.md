# Architecture assets

Place sanitized architecture screenshots and diagrams in this directory.

Recommended filename:

```text
paydao-architecture.png
```

Never include private keys, seed phrases, wallet JSON files, RPC tokens, TEE tokens, database passwords, or private URLs in an image.
